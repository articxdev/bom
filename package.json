{
  "name": "production-mgmt",
  "version": "1.0.0",
  "description": "Production Management Web App on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "tail": "npx wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
